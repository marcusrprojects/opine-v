const UserPrivacy = Object.freeze({
  PUBLIC: "public",
  PRIVATE: "private",
});

const CategoryPrivacy = Object.freeze({
  DEFAULT: "default",
  ONLY_ME: "only-me",
});

export { UserPrivacy, CategoryPrivacy };
